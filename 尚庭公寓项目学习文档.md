# 尚庭公寓项目学习文档
## 一、模块介绍与数据库设计
本文档针对移动端与后台管理的**我的房间**与**消息通知**模块进行了详细的设计说明。
### 1. 消息通知模块 (Notification)
用于承载系统内的业务提醒，如租约状态变更（签约、退租、续约）、系统公告以及预约消息提醒，支持针对具体租客或管理员发送业务消息。
#### 1.1 数据库结构 (notification)
| 字段名称        | 数据类型     | 描述                                                | 备注                          |
| --------------- | ------------ | --------------------------------------------------- | ----------------------------- |
| `id`            | bigint       | 主键ID                                              | 逻辑自增                      |
| `user_id`       | bigint       | 接收用户ID（区分租客或各类管理员）                    | 对应 UserInfo/SystemUser 表联查 |
| `user_type`     | tinyint(4)   | 接收对象应用端类型：1-租客移动端，2-管理员后台        |                               |
| `type`          | tinyint(4)   | 业务通知类型：1=签约 2=退租 3=续约 4=租约到期 5=系统公告 |                               |
| `business_id`   | bigint       | 业务ID（如引发通知的对应租约ID）                          |                               |
| `business_type` | tinyint(4)   | 业务主体类型：1=租约 2=看房预约 3=房间                |                               |
| `title`         | varchar(100) | 通知主标题                                          |                               |
| `content`       | varchar(500) | 通知正文内容                                        |                               |
| `is_read`       | tinyint(4)   | 是否已阅读：0=未读，1=已读                           | 配合主页小红点展示            |
| `read_time`     | datetime     | 标记已读的时间                                       |                               |
| `extra_data`    | json         | 扩展参数，存放前端需要解析的跳转路由/动作路由           |                               |
| `create_time`   | datetime     | 创建时间                                            | BaseEntity公共字段             |
| `update_time`   | datetime     | 更新时间                                            | BaseEntity公共字段             |
| `is_deleted`    | tinyint(4)   | 逻辑删除：0=未删除，1=已删除                         | BaseEntity公共字段             |
### 2. 我的房间模块 (MyRoom)
基于已完成租赁环节（即拥有生效中 `LeaseAgreement` 记录）的用户提供的履约服务，涵盖了当前房源状态查询、线上报修以及房租、水电等账单查看和缴费。
#### 2.1 数据库结构 
“我的房间”数据直接提取于已有的核心表，例如租约表 (`lease_agreement`)、房间信息表 (`room_info`) 等等，在此着重对所衍生的**报修记录**及**缴费记录**表进行罗列：
- **报修记录表 (repair_record)**
| 字段名称         | 数据类型     | 描述                       |
| ---------------- | ------------ | -------------------------- |
| `id`             | bigint       | 主键ID                     |
| `user_id`        | bigint       | 发起报修的用户ID           |
| `room_id`        | bigint       | 关联所在的房间ID           |
| `agreement_id`   | bigint       | 履带关系的租约ID           |
| `type`           | int          | 报修故障类型 (可走枚举)    |
| `description`    | varchar      | 故障现象图文描述详情       |
| `images`         | json/varchar | 现场图片的JSON阵列存储表示 |
| `status`         | int          | 当前工单进度 (1=待处理...) |
| `contact_phone`  | varchar      | 报修人的联络电话           |
| `appointment_time`| datetime    | 期待师傅上门排查时间       |
- **缴费记录表 (payment_record)**
| 字段名称         | 数据类型      | 描述                             |
| ---------------- | ------------- | -------------------------------- |
| `id`             | bigint        | 主键ID                           |
| `user_id`        | bigint        | 缴款用户ID                       |
| `agreement_id`   | bigint        | 相关租约ID                       |
| `room_id`        | bigint        | 对应发生账单房间ID               |
| `type`           | int           | 缴费项目类型（租金、水电、宽带等）|
| `amount`         | decimal(10,2) | 需缴费金额总计                   |
| `status`         | int           | 处理状态：待支付、已支付         |
| `due_date`       | datetime      | 最晚款项应缴限期                   |
| `pay_time`       | datetime      | 实际入账完成时间                   |
| `pay_method`     | varchar       | 支付通路（如微信支付、支付宝）   |
---
## 二、RESTful API 接口定义
以下皆采用 `Spring Boot` 约定的路径开发和响应封装，所有接口需要传递对应端的 Token 头以便 `LoginUserHolder` 进行鉴权拦截。
### 1. 消息通知相关接口
#### 1.1 获取分页消息列表
- **URL**: `GET /app/notification/page`
- **说明**: 用于消息中心主页面的无线滚动拉取或管理台表格呈现。
- **请求参数**:
  | 参数名   | 类型    | 必填 | 说明           |
  | -------- | ------- | ---- | -------------- |
  | current  | long    | 是   | 分页页码       |
  | size     | long    | 是   | 每页记录数     |
  | type     | Integer | 否   | 指定获取某类通知(按类型筛选) |
  | isRead   | Integer | 否   | 仅仅获取未读/已读的数据 |
- **响应**: 采用 `Page<Notification>` 分页装包数据。
#### 1.2 获取当前用户未读消息数量
- **URL**: `GET /app/notification/unread/count`
- **说明**: 移动端底层 Tabbar 小红点展示，登录载入即触发拉取。
- **响应**: 纯数字 `Result<Long>` 包装，代表未读总数。
#### 1.3 标记消息置为已读
- **URL**: `PUT /app/notification/read`
- **说明**: 对目标消息消除未读状态。
- **RequestBody 参数**: `Long[] notificationIds` (数组形式，可支持批量操作；如果留空则默认触发当前用户下**全部已读**机制)。
- **响应**: 成功布尔状态，无挂载数据。
---
### 2. 我的房间相关接口
隶属于移动端 `/app/myroom` 控制器下，是租客承租生命周期的重要入口。
#### 2.1 获取当前有效租房情况综合详情
- **URL**: `GET /app/myroom/info`
- **说明**: 根据当前登录用户获取最新的“进行中”或“签约完成待执行”租约，继而拼接具体的 `ApartmentName`、完整地址及关联房间图册组返回。
- **响应**: 返回聚合对象集 `Result<MyRoomInfoVo>`。
#### 2.2 获取当前主看合同详情
- **URL**: `GET /app/myroom/agreement`
- **说明**: 单独获取其当前的房屋服务条约及押金金额约定、服务期限。
- **响应**: 返回 `Result<LeaseAgreement>` 实体。
#### 2.3 提交报修申请单
- **URL**: `POST /app/myroom/repair/save`
- **说明**: 提交房屋内配套设施的报修和损坏照片。
- **RequestBody 参数**: `RepairSubmitVo`
  包括 `roomId`, `type`, `description`, `contactPhone`, `appointmentTime`, `images`(数组)。
- **响应**: 返回 `Result<Long>` 代表记录下发生成的工单总记录号。
#### 2.4 获取历史报修单分页列表
- **URL**: `GET /app/myroom/repair/list`
- **说明**: 用于展示用户下所有的历史保修与跟进状态。
- **请求参数**: `current`, `size`
- **响应**: `Result<Page<RepairRecordVo>>` 数据流列表。
#### 2.5 获取居住期间缴费记录分页列表
- **URL**: `GET /app/myroom/payment/list`
- **说明**: 用以汇总租务履约中已发生的月结账单及历届开销日志。
- **请求参数**: `current`, `size`
- **响应**: `Result<Page<PaymentRecordVo>>`
#### 2.6 获取累积未缴金额总结
- **URL**: `GET /app/myroom/payment/unpaid/total`
- **说明**: 检查并返回目前“逾期+本期应缴但未缴”的总金额求和。
- **响应**: `Result<BigDecimal>` 金额类计算响应。
